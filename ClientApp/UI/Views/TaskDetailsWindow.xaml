<Window x:Class="ClientApp.UI.Views.TaskDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:infra="clr-namespace:ClientApp.Infrastructure"
        mc:Ignorable="d"
        Title="{infra:TranslateExtension Key=TaskDetails}" 
        MinHeight="700" MinWidth="900"
        Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource PrimaryThemeBrush}" Padding="15">
            <Grid>
                <TextBlock Text="{Binding TasksOnDateHeader}" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                <ComboBox HorizontalAlignment="Right" VerticalAlignment="Center"
                          SelectedIndex="0" SelectionChanged="LanguageComboBox_SelectionChanged">
                    <ComboBoxItem Content="English" Tag="en" />
                    <ComboBoxItem Content="Русский" Tag="ru" />
                </ComboBox>
            </Grid>
        </Border>
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="220" MaxWidth="350"/>
                <ColumnDefinition Width="2.5*" MinWidth="450"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{StaticResource SecondaryBackgroundBrush}" CornerRadius="4" Padding="10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="{infra:TranslateExtension Key=Tasks}" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox ItemsSource="{Binding TaskList}" 
                     SelectedItem="{Binding SelectedListItem, Mode=TwoWay}"
                     BorderThickness="0" 
                     HorizontalContentAlignment="Stretch">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="28">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding Title}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Converter={StaticResource TypeCheckConverter}, ConverterParameter=System.String}" Value="True">
                                                            <Setter Property="Text" Value="{Binding}"/>
                                                            <Setter Property="FontStyle" Value="Italic"/>
                                                            <Setter Property="Foreground" Value="Gray"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Button Grid.Column="1" Content="×" FontSize="16" FontWeight="Bold"
                                    Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent" Foreground="{StaticResource DangerBrush}" 
                                    Padding="5,0" Cursor="Hand" VerticalAlignment="Center">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Converter={StaticResource TypeCheckConverter}, ConverterParameter=System.String}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.7"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </DockPanel>
            </Border>
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="15,0,0,0" 
                          IsEnabled="{Binding IsDetailsEnabled}">
                <StackPanel DataContext="{Binding SelectedTask}">
                    <CheckBox Content="{infra:TranslateExtension Key=MarkAsCompleted}"
                              IsChecked="{Binding DataContext.IsTaskCompleted, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"
                              Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibilityConverter}}"
                              Margin="0,0,0,15" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{infra:TranslateExtension Key=Title}" FontWeight="Bold"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                    <TextBlock Text="{infra:TranslateExtension Key=Description}" FontWeight="Bold"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="100" Margin="0,5,0,10" VerticalScrollBarVisibility="Auto"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{infra:TranslateExtension Key=DueDate}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding DueDate, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                        <TextBlock Grid.Column="2" Text="{infra:TranslateExtension Key=Time}" FontWeight="Bold" Margin="20,0,5,0" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="3" 
                                 Text="{Binding DataContext.DueTime, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="50"/>
                    </Grid>
                    <Border BorderBrush="{StaticResource BorderBrush}" Background="{StaticResource SecondaryBackgroundBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,10">
                        <StackPanel>
                            <TextBlock Text="{infra:TranslateExtension Key=Comments}" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="150" Margin="0,0,0,10">
                                <ItemsControl ItemsSource="{Binding DataContext.Comments, RelativeSource={RelativeSource AncestorType=Window}}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="5" Margin="0,0,0,5" Background="#F9F9F9" CornerRadius="3">
                                                <StackPanel>
                                                    <TextBlock TextWrapping="Wrap">
                                                        <Run Text="{Binding Author.FullName}" FontWeight="Bold"/>
                                                        <Run Text=" - "/>
                                                        <Run Text="{Binding SentAt, StringFormat='g'}" Foreground="Gray"/>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="5,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <Grid IsEnabled="{Binding Id, Converter={StaticResource IntToBoolConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding DataContext.NewCommentText, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" Height="50"/>
                                <Button Grid.Column="1" Content="{infra:TranslateExtension Key=AddCommentButton}" 
                                        Command="{Binding DataContext.AddCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Bottom"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <Border Grid.Row="2" Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusMessageColor}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{infra:TranslateExtension Key=SaveButton}" Command="{Binding SaveTaskCommand}" Width="120" Margin="0,0,10,0" IsDefault="True"/>
                    <Button Content="{infra:TranslateExtension Key=CloseButton}" IsCancel="True" Width="120" Style="{StaticResource CancelButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>