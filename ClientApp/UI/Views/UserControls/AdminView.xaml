<UserControl x:Class="ClientApp.UI.Views.UserControls.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:infra="clr-namespace:ClientApp.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False"
              SelectedItem="{Binding SelectedUser}" IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="{infra:TranslateExtension Key=UsernameField}" Binding="{Binding Username}" Width="1.5*"/>
            <DataGridTextColumn Header="{infra:TranslateExtension Key=FullNameField}" Binding="{Binding FullName}" Width="2*"/>
            <DataGridTextColumn Header="{infra:TranslateExtension Key=TaskCount}" Binding="{Binding TaskCount}" Width="*"/>
            <DataGridTemplateColumn Width="Auto">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.OpenAssignmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                ToolTip="{infra:TranslateExtension Key=ManageTasksButton}"
                                Background="Transparent" Padding="5">
                            <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource AccentBrush}"/>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Width="Auto">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                ToolTip="{infra:TranslateExtension Key=DeleteUserToolTip}"
                                Background="Transparent" Padding="5">
                            <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource DangerBrush}"/>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>