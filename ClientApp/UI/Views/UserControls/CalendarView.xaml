<UserControl x:Class="ClientApp.UI.Views.UserControls.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientApp.UI.Views.UserControls"
             xmlns:infra="clr-namespace:ClientApp.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="10" Background="#F0F0F0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="&lt;" Command="{Binding PreviousMonthCommand}" Margin="0,0,20,0" FontWeight="Bold" Width="40" Height="40"/>
                <TextBlock Text="{Binding MonthYearHeader}" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Content=">" Command="{Binding NextMonthCommand}" Margin="20,0,0,0" FontWeight="Bold" Width="40" Height="40"/>
            </StackPanel>
        </Border>
        <UniformGrid Grid.Row="1" Rows="1" Background="WhiteSmoke">
            <TextBlock Text="{infra:TranslateExtension Key=Monday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold"/>
            <TextBlock Text="{infra:TranslateExtension Key=Tuesday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold"/>
            <TextBlock Text="{infra:TranslateExtension Key=Wednesday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold"/>
            <TextBlock Text="{infra:TranslateExtension Key=Thursday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold"/>
            <TextBlock Text="{infra:TranslateExtension Key=Friday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold"/>
            <TextBlock Text="{infra:TranslateExtension Key=Saturday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold" Foreground="DarkRed"/>
            <TextBlock Text="{infra:TranslateExtension Key=Sunday}" HorizontalAlignment="Center" Padding="5" FontWeight="Bold" Foreground="DarkRed"/>
        </UniformGrid>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Days}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7" Rows="6"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0.5">
                        <Grid>
                            <TextBlock Text="{Binding DayNumber}" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left"
                                       FontWeight="{Binding IsToday, Converter={StaticResource BoolToFontWeightConverter}}"/>
                            <Border Width="28" Height="28" CornerRadius="14" Background="White" BorderBrush="Green" BorderThickness="2"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Visibility="{Binding ShowCompletedIcon, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="+" Foreground="Green" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ItemsControl ItemsSource="{Binding DisplayTasks}" Margin="5,25,5,5">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <local:TaskCellControl />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Opacity="0"
                                    Command="{Binding DataContext.DaySelectedCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding IsEmpty, Converter={StaticResource InverseBoolConverter}}"
                                    ToolTip="{Binding TaskTitlesTooltip}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>