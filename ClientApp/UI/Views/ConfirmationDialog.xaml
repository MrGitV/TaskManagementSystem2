<Window x:Class="ClientApp.UI.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:infra="clr-namespace:ClientApp.Infrastructure"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        MinWidth="350" MaxWidth="500"
        Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,25" FontSize="14" Foreground="{StaticResource TextBrush}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{infra:TranslateExtension Key=YesButton}" Command="{Binding YesCommand}" Width="90" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="{infra:TranslateExtension Key=NoButton}" Command="{Binding NoCommand}" Width="90" IsCancel="True" Style="{StaticResource CancelButton}"/>
        </StackPanel>
    </Grid>
</Window>