<Window x:Class="ClientApp.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:infra="clr-namespace:ClientApp.Infrastructure"
        mc:Ignorable="d"
        Title="{infra:TranslateExtension Key=LoginHeader}"
        Height="500" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="40">
            <TextBlock Text="{infra:TranslateExtension Key=AppTitle}" 
                       Style="{StaticResource HeaderTextBlock}" 
                       HorizontalAlignment="Center" Margin="0,0,0,30" FontSize="20"/>
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="{StaticResource DangerBrush}" 
                       TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,10" 
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            <TextBlock Text="{infra:TranslateExtension Key=UsernameField}" Margin="0,10,0,5" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="{infra:TranslateExtension Key=PasswordField}" Margin="0,10,0,5" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" />
            <Button Content="{infra:TranslateExtension Key=LoginButton}" 
                    Command="{Binding LoginCommand}" Margin="0,30,0,0" IsDefault="True"/>
            <Button Content="{infra:TranslateExtension Key=ExitButton}" 
                    Click="ExitButton_Click" HorizontalAlignment="Center" Margin="0,10,0,0" 
                    Style="{StaticResource CancelButton}" Width="100"/>
        </StackPanel>
        <ComboBox HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"
                  SelectedIndex="0" SelectionChanged="LanguageComboBox_SelectionChanged">
            <ComboBoxItem Content="English" Tag="en" />
            <ComboBoxItem Content="Русский" Tag="ru" />
        </ComboBox>
    </Grid>
</Window>